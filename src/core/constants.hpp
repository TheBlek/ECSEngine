#pragma once
#include <cstdint>

using Component = std::uint16_t;
const Component MAX_COMPONENTS = 30;
const Component MAX_SYSTEMS = 30;

using Entity = std::uint16_t;
const Entity MAX_ENTITIES = 5000;
